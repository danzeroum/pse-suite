// E-13: terceiro-do-terceiro. Este host vive DENTRO da dependencia e nunca
// aparece no codigo de primeira parte, entao S-04 nao o enxerga.
export function track(evt) {
  navigator.sendBeacon("https://telemetria.rastreador.example.net/collect",
                       JSON.stringify(evt));
}
